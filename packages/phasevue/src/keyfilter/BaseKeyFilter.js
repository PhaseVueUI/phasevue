import BaseDirective from '@phasevueui/core/basedirective';
import KeyFilterStyle from 'phasevue/keyfilter/style';

const BaseKeyFilter = BaseDirective.extend({
    style: KeyFilterStyle
});

export default BaseKeyFilter;
