import { style } from '@phasevueui/styles-core/buttongroup';
import BaseStyle from '@phasevueui/core/base/style';

const classes = {
    root: 'p-buttongroup p-component'
};

export default BaseStyle.extend({
    name: 'buttongroup',
    style,
    classes
});
