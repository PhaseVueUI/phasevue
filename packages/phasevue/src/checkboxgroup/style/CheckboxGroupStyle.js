import { style } from '@phasevueui/styles-core/checkboxgroup';
import BaseStyle from '@phasevueui/core/base/style';

const classes = {
    root: 'p-checkbox-group p-component'
};

export default BaseStyle.extend({
    name: 'checkboxgroup',
    style,
    classes
});
