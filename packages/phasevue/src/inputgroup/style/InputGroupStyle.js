import { style } from '@phasevueui/styles-core/inputgroup';
import BaseStyle from '@phasevueui/core/base/style';

const classes = {
    root: 'p-inputgroup'
};

export default BaseStyle.extend({
    name: 'inputgroup',
    style,
    classes
});
